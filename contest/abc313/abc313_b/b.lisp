(let* ((n (read))
       (m (read))
       (abm (loop repeat m collect (list (read) (read))))
       (visited (make-array n :initial-element nil)))
  (loop for i below n
        do (loop while ))
  )
